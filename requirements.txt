# =============================================================================
# ClipForge — single combined requirements file
#
# Pulls together everything the three vendored projects need so users only
# run `pip install -r requirements.txt` once.
# =============================================================================

# ---- Web framework ----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
jinja2==3.1.5
aiofiles==24.1.0

# ---- Video download & metadata ---------------------------------------------
yt-dlp>=2024.12.13
ffmpeg-python==0.2.0
imageio[ffmpeg]==2.36.1

# ---- Transcription (Whisper) -----------------------------------------------
faster-whisper==1.0.3
# torch CPU build is large; install the GPU build manually if you have CUDA.
# torch==2.4.1

# ---- Subtitles -------------------------------------------------------------
pysubs2==1.8.0

# ---- LLM providers ---------------------------------------------------------
google-genai>=1.3.0
openai>=1.50.0

# ---- Image / video inspection ---------------------------------------------
Pillow==11.0.0
numpy==1.26.4

# ---- HTML scraping for YouTube metadata ------------------------------------
requests==2.33.0
beautifulsoup4==4.12.3
